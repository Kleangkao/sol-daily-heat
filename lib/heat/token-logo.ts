/** DexScreener CDN token icon for Solana mints (display-only). */
export function solanaTokenLogoUrl(mint: string): string {
  return `https://dd.dexscreener.com/ds-data/tokens/solana/${mint}.png`;
}
