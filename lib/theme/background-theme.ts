/**
 * Toggle Solana gradient experiment. Set to false to restore the beach atmosphere.
 * Desktop (lg+): split layout — legacy beach header + Solana gradient body.
 */
export const USE_SOLANA_GRADIENT_THEME = false;

export const SOLANA_GRADIENT = {
  purple: "#9945FF",
  green: "#14F195",
} as const;
