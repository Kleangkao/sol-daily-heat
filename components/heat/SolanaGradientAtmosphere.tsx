/** Split background: legacy beach header zone + Solana gradient body (desktop). */
export default function SolanaGradientAtmosphere() {
  return (
    <div className="solana-gradient-atmosphere" aria-hidden>
      <div className="solana-gradient-atmosphere__base" />
    </div>
  );
}
